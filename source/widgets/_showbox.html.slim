.col-md-4.col-sm-6.well
  .portfolio-item
    .portfolio-image
      a[href="#{item.url}"] = image_tag item.thumb.url

    .portfolio-info-fade
      ul
        li.portfolio-project-name= l item.title
        li= l item.subtitle
        li= l item.summary
        li.read-more
          a.btn.btn-theme[href="#{locale_prefix}/#{item.url}"]= t :read_more
